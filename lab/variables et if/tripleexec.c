#include <stdio.h>
#include <stdlib.h>

int triple(int nombre)
{
    return 3 * nombre;
}

int main (int argc, char*arv[])


nombredeVies = 5;
nombredeVies = 4

//la variable nombre de vies stocke la valeur q'on lui donne


// && || ! 

// if age(!(age < 18))

//age inferior to 18 so NO => meaning age supperior to 18 